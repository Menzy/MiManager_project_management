name: Deploy Vite React App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Prepare SSH Key for Deployment
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          printf "%s" "$SSH_PRIVATE_KEY" > key.pem
          chmod 600 key.pem

      - name: Deploy to VPS
        run: |
          scp -i key.pem -o StrictHostKeyChecking=no -r dist/* root@172.236.1.252:/var/www/MiManager_project_management
          rm key.pem
